patch:
  schema_list:
    - schema: rime_frost_double_pinyin_flypy    # 小鹤双拼

  menu/page_size: 6  # 候选词个数

  switcher/hotkeys: []

  ascii_composer/switch_key/Shift_L: commit_code

  # 使用用户文件夹下的 punctuation.yaml 作为符号配置
  punctuator/half_shape:
    __include: punctuation:/half_shape
  punctuator/full_shape:
    __include: punctuation:/full_shape

  key_binder/bindings:
    # Tab / Shift+Tab 切换光标至下/上一个拼音
    - { when: composing, accept: Shift+Tab, send: Shift+Left }
    - { when: composing, accept: Tab, send: Shift+Right }
    # 翻页
    - { when: has_menu, accept: Control+i, send: Page_Up }
    - { when: has_menu, accept: Control+o, send: Page_Down }
    # 清除自造词
    - { when: has_menu, accept: Control+k, send: Shift+Delete }
    # 清空输入内容
    - { when: composing, accept: Control+u, send: Escape }
    # 删除输出字符
    - { when: composing, accept: Control+h, send: BackSpace }
    - { when: composing, accept: Control+d, send: Delete }
    # 移动到行首行尾
    - { when: composing, accept: Control+a, send: Home }
    - { when: composing, accept: Control+e, send: End }
    # 方向移动
    - { when: composing, accept: Control+p, send: Up }
    - { when: composing, accept: Control+n, send: Down }
    - { when: composing, accept: Control+f, send: Right }
    - { when: composing, accept: Control+b, send: Left }
    # 将小键盘 0~9 . 映射到主键盘，数字金额大写的 Lua 如 R1234.5678 可使用小键盘输入
    - {accept: KP_0, send: 0, when: composing}
    - {accept: KP_1, send: 1, when: composing}
    - {accept: KP_2, send: 2, when: composing}
    - {accept: KP_3, send: 3, when: composing}
    - {accept: KP_4, send: 4, when: composing}
    - {accept: KP_5, send: 5, when: composing}
    - {accept: KP_6, send: 6, when: composing}
    - {accept: KP_7, send: 7, when: composing}
    - {accept: KP_8, send: 8, when: composing}
    - {accept: KP_9, send: 9, when: composing}
    - {accept: KP_Decimal, send: period, when: composing}
